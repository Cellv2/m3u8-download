describe("converter", () => {
    it("should merge the mpegts files into an mp4", () => {
        // just an mp4? options to define what to output?
    });

    it("should ensure that ffmpeg is available on the system", () => {});
});
