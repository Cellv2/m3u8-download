describe("downloadFile", () => {
    it("should return an array of mpegts files from the m3u8", () => {

    });

    it("should return an empty array if no mpegts files are found in the m3u8", () => {

    })
});
